import type { types, directions, orientations } from "./constants";

export type Type = typeof types[number];
export type Direction = typeof directions[number];
export type Orientation = typeof orientations[number];