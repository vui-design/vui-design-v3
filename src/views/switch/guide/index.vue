<template>
  <div>
    <h2 id="guide-switch-when-to-use">何时使用</h2>
    <ul>
      <li>需要表示开关状态或仅有两种状态之间的切换时；</li>
      <li>需要操作后立即生效时；</li>
      <li>和 <code>Checkbox</code> 的区别是，切换 <code>Switch</code> 会直接触发状态改变，而 <code>Checkbox</code> 一般用于状态标记，需要和提交操作配合。</li>
    </ul>
  </div>
</template>