<template>
  <div>
    <h2 id="guide-checkbox-when-to-use">何时使用</h2>
    <ul>
      <li>需要从一个数据集中选择多个选项时；</li>
      <li>需要对两种状态进行切换时（选中或未选中，打开或关闭），可单独使用多选框；</li>
      <li>需要一个标记控件，通过触发操作按钮后才生效时；</li>
      <li>单独使用可以表示两种状态之间的切换，和 <code>Switch</code> 类似。区别在于切换 <code>Switch</code> 会直接触发状态改变，而 <code>Switch</code> 一般用于状态标记，需要和提交操作配合。</li>
    </ul>
  </div>
</template>